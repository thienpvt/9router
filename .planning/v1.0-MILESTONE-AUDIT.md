---
milestone: v1.0
audited: 2026-07-08T00:58:08Z
status: tech_debt
scores:
  requirements: 17/17
  phases: 4/4
  integration: 17/17
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 02-thinking-block-fidelity
    items:
      - "W1: applyThinking short-circuit (thinkingUnified.js:275) checks provider===\"ollama\" only — NOT ollama-local. Under the claude transport, ollama-local runs the general path: strips output_config (OpenAI-shaped) + adds budget_tokens to thinking. Strict reading of THINK-02 ('reach ollama unchanged', no provider qualification) is a fidelity asymmetry on the local path; cloud path holds strictly. Documented via ponytail comment at thinkingUnified.js:273-274 (upgrade: lift into PROVIDERS[ollama].quirks or a capability flag if a second native-claude provider lands) + Phase 4 VAL-01 test deliberately scoped the ollama-local assertion to messages/system/tools (omits thinking/output_config)."
      - "W2: VAL-02 round-trip is a mocked inline 12-event /v1/messages SSE sequence, NOT recorded from a live ollama cloud endpoint. Runtime SSE-shape fidelity (unexpected ollama fields, non-Anthropic event variants) unproven. Accepted per VAL-02 requirement text ('or a mock of the /v1/messages contract'). Live round-trip is a *.real.test.js under tests/translator/real/, skipped without credentials."
      - "W3: No test drives chatCore.js end-to-end with a mocked executor. All Phase 1-4 tests are unit-level (translateRequest/translateResponse/resolveTransport/buildUrl/applyThinking in isolation). COMP-04 header wiring (DefaultExecutor.buildHeaders applying rt.auth + rt.headers) verified structurally via grep + buildHeaders source, not dynamically. Phase 1 01-02-SUMMARY coverage D5 explicitly defers end-to-end header assertion."
  - phase: cross-milestone
    items:
      - "COMP-04 cloud auth scheme: wired x-api-key raw by convention (matching GLM). Live auth-success probe against /v1/messages remains a *.real.test.js (skipped without credentials). Not a blocker — code path verified by inspection + integration check."
---

# Milestone v1.0 — Audit: Claude→Ollama Passthrough

**Audited:** 2026-07-08
**Status:** tech_debt (no blockers; documented deferred items need review)

## Summary

All 4 phases complete + verified `passed`. All 17/17 v1 requirements satisfied (REQUIREMENTS.md traceability + per-phase VERIFICATION.md + SUMMARY coverage cross-referenced). Cross-phase integration verified — 9/9 wiring connections WIRED, 3/3 E2E flows hold end-to-end, 0 orphaned requirements, 0 broken flows.

Integration checker surfaced 3 warnings, all documented/accepted ceilings with named upgrade paths (not hidden gaps). Classified `tech_debt` rather than `gaps_found` because no requirement is silently unmet — each ceiling is visible in the code (ponytail comments), the tests (deliberate scope relaxation), or the planning artifacts (CONTEXT decisions).

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | Traceability | VERIFICATION | SUMMARY coverage | Status |
|--------|------|--------------|--------------|-------------------|--------|
| PASS-01 | 1 | Complete | passed | ollama-claude-transport Contract A | ✓ satisfied |
| PASS-02 | 1 | Complete | passed | ollama-claude-transport Contract A-local + C | ✓ satisfied |
| PASS-03 | 1 | Complete | passed | ollama-claude-transport Contract B/C + VAL-03 | ✓ satisfied |
| COMP-04 | 1 | Complete | passed | DefaultExecutor.buildHeaders rt.auth (static) | ✓ satisfied (W3: not driven dynamically) |
| THINK-01 | 2 | Complete | passed | thinking-passthrough THINK-01 | ✓ satisfied |
| THINK-02 | 2 | Complete | passed | thinking-passthrough THINK-02 (cloud) | ✓ satisfied cloud; ⚠ local (W1) |
| THINK-03 | 2 | Complete | passed | thinking-passthrough THINK-03/03b/03c | ✓ satisfied |
| BLK-01 | 2 | Complete | passed | block-fidelity BLK-01 | ✓ satisfied |
| BLK-02 | 2 | Complete | passed | block-fidelity BLK-02 | ✓ satisfied |
| BLK-03 | 2 | Complete | passed | block-fidelity BLK-03/03b/03c (hasValidContent fix) | ✓ satisfied |
| BLK-04 | 2 | Complete | passed | block-fidelity BLK-04 + round-trip VAL-02 | ✓ satisfied |
| COMP-01 | 3 | Complete | passed | compat COMP-01a/b/c/d | ✓ satisfied |
| COMP-02 | 3 | Complete | passed | compat COMP-02a/b/c | ✓ satisfied |
| COMP-03 | 3 | Complete | passed | compat COMP-03a/b/c/d/cache-creation/e | ✓ satisfied |
| VAL-01 | 4 | Complete | passed | regression VAL-01 (structuredClone snapshot) | ✓ satisfied |
| VAL-02 | 4 | Complete | passed | round-trip VAL-02 (mocked SSE) | ✓ satisfied (W2: mock not live) |
| VAL-03 | 4 | Complete | passed | regression VAL-03 (routing chain) | ✓ satisfied |

**Orphaned requirements:** 0 (all 17 REQ-IDs appear in at least one phase VERIFICATION + test).

## Phase Verification Status

| Phase | Name | VERIFICATION status |
|-------|------|---------------------|
| 1 | Passthrough Transport & Auth | passed |
| 2 | Thinking & Block Fidelity | passed |
| 3 | Compatibility & Fallback | passed |
| 4 | Validation & Tests | passed |

## Cross-Phase Integration (9/9 WIRED)

| # | Connection | Status |
|---|-----------|--------|
| 1 | Phase 1 transports[] → chatCore resolveTransport → targetFormat=claude | WIRED |
| 2 | chatCore → translator same-format skip (index.js:78) | WIRED |
| 3 | Phase 1 → Phase 2 applyThinking ollama-claude short-circuit + WR-01 normalization | WIRED (cloud; W1 local) |
| 4 | chatCore → prepareClaudeRequest (cache_control rewrite + hasValidContent image/document) | WIRED |
| 5 | chatCore → executor buildUrl (Phase 1 WR-01/02/03 hardened, dual-path) | WIRED |
| 6 | chatCore → executor buildHeaders (COMP-04 x-api-key raw + CLAUDE_API_HEADERS) | WIRED (static; W3 dynamic) |
| 7 | chatCore → translateResponse same-format passthrough (BLK-04, COMP-02, VAL-02 — one mechanism, 3 dependents) | WIRED |
| 8 | extractUsage Claude-shape branch (COMP-03) + native ollama NDJSON fallback | WIRED |
| 9 | OpenAI fallback (PASS-03, VAL-03) — resolveTransport openai→null → /api/chat | WIRED |

## E2E Flows (3/3 hold)

- Claude client → ollama cloud `/v1/messages` — HOLDS (11 checkpoints traced)
- Claude client → ollama local `<host>/v1/messages` — HOLDS (host-substituted; THINK-02 strict-reading caveat per W1)
- OpenAI client → ollama `/api/chat` — HOLDS (fallback path preserved, byte-identical transport object)

## Nyquist Coverage

N/A — `workflow.nyquist_validation` is enabled in config but research was skipped for every phase (CONTEXT pinpoints the code). VALIDATION.md not created (no-research path). No action required — the runnable self-checks per phase (the vitest suites) serve the equivalent regression-guard role for this transport-layer milestone.

## Tech Debt (review before completing)

### W1 — applyThinking short-circuit scope (THINK-02 strict reading for ollama-local)
`thinkingUnified.js:275` predicate is `provider === "ollama"` only. For `ollama-local` under the claude transport, the short-circuit does NOT fire — the general path runs, stripping `output_config` and adding `budget_tokens` to `thinking`. Strict reading of THINK-02 ("reach ollama unchanged" without provider qualification) is a fidelity asymmetry on the local path; the cloud path holds strictly.
- Documented: ponytail comment `thinkingUnified.js:273-274` (upgrade path: lift into `PROVIDERS[ollama].quirks` or a capability flag).
- Test relaxation: Phase 4 VAL-01 `Contract VAL-01 local` deliberately scoped to `messages`/`system`/`tools` (omits `thinking`/`output_config`).
- **Decision needed:** accept as documented ceiling (complete milestone) OR close via a quick fix (broaden the predicate to `provider === "ollama" || provider === "ollama-local"` + extend the test).

### W2 — VAL-02 mocked (not live) round-trip
VAL-02 uses an inline-constructed 12-event SSE sequence, not a recorded live stream. Runtime SSE-shape fidelity unproven. Accepted per VAL-02 requirement text. Live round-trip = a `*.real.test.js` skipped without credentials.

### W3 — No chatCore end-to-end test with mocked executor
All tests are unit-level. COMP-04 header wiring verified structurally, not dynamically. Phase 1 01-02-SUMMARY coverage D5 explicitly deferred.

### Cross-milestone — COMP-04 live auth probe
x-api-key raw wired by convention. Live auth-success against `/v1/messages` remains a `*.real.test.js`. Code path verified by inspection + integration check.

## Recommendation

The single item that could warrant a pre-completion fix is **W1** — it's a one-line predicate broadening + a test extension, and it closes the strict-reading gap on THINK-02 for `ollama-local`. W2/W3/cross-milestone are accepted ceilings (mocked/live-test boundaries explicitly out of scope per REQUIREMENTS + CONTEXT decisions).

Options:
- **A. Complete milestone** — accept the documented tech debt (W1 is a ponytail-named ceiling; W2/W3 are accepted test-scope boundaries).
- **B. Close W1 first** — broadening `provider === "ollama"` → `provider === "ollama" || provider === "ollama-local"` in the applyThinking short-circuit + extending the ollama-local thinking test, then complete. Small, low-risk.

---
*Audit run: 2026-07-08 by /gsd-audit-milestone (autonomous lifecycle)*