---
phase: 04-validation-tests
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/ollama-claude-regression.test.js
  - tests/unit/ollama-claude-round-trip.test.js
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-03
user_setup: []

must_haves:
  truths:
    - "VAL-01: a Claude-format request to ollama resolves targetFormat=claude and the dispatched body is structurally identical to the client's body (no openai intermediate hop)"
    - "VAL-02: a mocked /v1/messages SSE round-trip reconstructs the full Anthropic event sequence with thinking + tool_use blocks surviving end-to-end"
    - "VAL-03: a non-Claude openai-format request to ollama resolves no claude transport and routes through /api/chat unchanged"
  artifacts:
    - tests/unit/ollama-claude-regression.test.js
    - tests/unit/ollama-claude-round-trip.test.js
  key_links:
    - "translateRequest CLAUDE→CLAUDE same-format skip (open-sse/translator/index.js:78) preserves client body — VAL-01"
    - "translateResponse CLAUDE→CLAUDE identity passthrough (open-sse/translator/index.js:152 returns [chunk]) — VAL-02"
    - "resolveTransport(ollama,openai)===null forces /api/chat fallback — VAL-03"
---

<objective>
Lock the Phase 1-3 passthrough contract into a formal regression + round-trip + fallback-guard test suite (VAL-01/02/03). Test-only — no open-sse source changes.

Purpose: Prove the claude→ollama passthrough holds against future refactors and non-Claude routing is unaffected. VAL-02 is the new deliverable (mocked /v1/messages SSE round-trip); VAL-01/03 consolidate + clearly-label the regression + fallback assertions already proven in Phase 1.
Output: Two self-contained vitest files under tests/unit/ that pass green on a plain checkout (no network, no mocks, no new deps).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-tests/04-CONTEXT.md
@.planning/phases/04-validation-tests/04-PATTERNS.md

# Analogs — the harness patterns to copy
@tests/translator/golden-response-stream.test.js
@tests/unit/ollama-claude-transport.test.js

# Prior phase test files — reference (do NOT duplicate their assertions)
@tests/unit/ollama-claude-block-fidelity.test.js
@tests/unit/ollama-claude-compat.test.js

# Contract under test — same-format skip (request:78, response:152)
@open-sse/translator/index.js
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: VAL-01 regression + VAL-03 fallback guard test</name>
  <files>tests/unit/ollama-claude-regression.test.js</files>
  <read_first>
    - tests/unit/ollama-claude-transport.test.js (analog — resolveTransport + buildUrl harness, imports from open-sse/services/provider.js + open-sse/executors/ollama-local.js)
    - open-sse/translator/index.js lines 52-146 (translateRequest — line 78 same-format skip, line 118-121 prepareClaudeRequest finalization for claude target)
    - .planning/phases/04-validation-tests/04-PATTERNS.md (VAL-01 body-identity assertion block + VAL-03 reference guard block)
    - tests/unit/ollama-claude-block-fidelity.test.js (Phase 2 — already proves field-level identity; VAL-01 formalizes as a structural deep-equal, do not re-run its assertions)
    - tests/unit/ollama-claude-compat.test.js (Phase 3 — WR-01 removed a duplicate fallback guard; VAL-03 owns the clearly-named guard now)
  </read_first>
  <behavior>
    - Contract VAL-01: translateRequest(FORMATS.CLAUDE, FORMATS.CLAUDE, model, body, false, null, "ollama") with a body containing {model, messages, system, thinking:{type:"enabled"}, output_config:{effort:"high"}, tools} returns a result whose core fields (messages, system, thinking, output_config) match the input — toMatchObject(body). The result has NO openai-intermediate markers (no "choices" array, no "messages"-as-openai rewrite) — expect(result).not.toHaveProperty("choices").
    - Contract VAL-01 local: same assertion holds for provider "ollama-local".
    - Contract VAL-01 cache_control tolerance: when body.messages contain a cache_control marker, prepareClaudeRequest (line 118, runs because targetFormat===CLAUDE) may rewrite strip or move cache_control — the assertion MUST scope the deep-equal to the fields that pass through verbatim (messages content blocks, system, thinking, output_config, tools) and NOT assert cache_control identity. Use toMatchObject on a subset that excludes cache_control, OR strip cache_control from both sides before comparing. Document this scoping in a comment.
    - Contract VAL-03: resolveTransport("ollama","openai")===null AND resolveTransport("ollama-local","openai")===null — no claude transport matches openai-format, so it falls through to /api/chat.
    - Contract VAL-03 buildUrl fallback: new OllamaLocalExecutor().buildUrl("", true, 0, null) === "http://localhost:11434/api/chat" — the openai path's transport object uses /api/chat. This is a clearly-named guard, not a duplicate of Phase 1 Contract C (Phase 1 proves buildUrl mechanics; Phase 4 labels it as the VAL-03 fallback guard).
  </behavior>
  <action>
    Create tests/unit/ollama-claude-regression.test.js. Use the Phase 1 transport test harness pattern: import { describe, expect, it } from "vitest"; import { resolveTransport } from "../../open-sse/services/provider.js"; import OllamaLocalExecutor from "../../open-sse/executors/ollama-local.js"; import { translateRequest } from "../../open-sse/translator/index.js"; import { FORMATS } from "../../open-sse/translator/formats.js".

    describe("Phase 4: VAL-01 regression + VAL-03 fallback guard"). Three it() blocks under it, each named "Contract VAL-01: ..." / "Contract VAL-01 local: ..." / "Contract VAL-03: ...".

    VAL-01 body: const body = { model: "claude-sonnet-4-5", messages: [{ role: "user", content: "hi" }], system: "You are helpful", thinking: { type: "enabled" }, output_config: { effort: "high" }, tools: [{ name: "get_weather", input_schema: {} }] }. Call translateRequest(FORMATS.CLAUDE, FORMATS.CLAUDE, body.model, body, false, null, "ollama"). Assert expect(result).toMatchObject({ messages: body.messages, system: body.system, thinking: body.thinking, output_config: body.output_config, tools: body.tools }) — scoped deep-equal on the passthrough fields, NOT a full toEqual (prepareClaudeRequest at line 118 may add/normalize headers, cache_control, anthropic-version). Assert expect(result).not.toHaveProperty("choices") — negative proof no openai intermediate. Repeat for provider "ollama-local" in a second it block.

    For the cache_control scoping: do NOT add cache_control to the input body in VAL-01's base assertion (keep the assertion clean). Add a SEPARATE it block "Contract VAL-01 cache_control: prepareClaudeRequest may rewrite cache_control; passthrough fields unaffected" that sets messages:[{role:"user",content:[{type:"text",text:"hi",cache_control:{type:"ephemeral"}}]}], runs translateRequest, and asserts the text content survives (expect(result.messages[0].content[0].text).toBe("hi")) WITHOUT asserting cache_control shape — proving the passthrough doesn't drop the message even when cache_control is normalized. This documents the known prepareClaudeRequest interaction (CONTEXT.md line 23 + Phase 3 COMP-01).

    VAL-03: single it block "Contract VAL-03: openai-format request to ollama routes /api/chat (no claude transport matches)". Assert resolveTransport("ollama","openai")===null, resolveTransport("ollama-local","openai")===null, new OllamaLocalExecutor().buildUrl("",true,0,null)==="http://localhost:11434/api/chat". Add a comment: "Phase 1 Contract B+C proves the mechanics; this is the Phase 4 named guard locking the fallback."

    No try/catch (pure-function; vitest surfaces throws as failures). No mocks, no network. No fenced code in this action — names + signatures only.
  </action>
  <verify>
    <automated>cd tests && npx vitest run unit/ollama-claude-regression.test.js</automated>
  </verify>
  <done>
    - `cd tests && npx vitest run unit/ollama-claude-regression.test.js` exits 0 (all Contract VAL-01 + VAL-03 blocks pass).
    - VAL-01: translateRequest CLAUDE→CLAUDE result matches input on {messages, system, thinking, output_config, tools} and has no "choices" property — for both "ollama" and "ollama-local".
    - VAL-01 cache_control scoping block documents the prepareClaudeRequest interaction without false-asserting cache_control identity.
    - VAL-03: resolveTransport(ollama/ollama-local, openai)===null AND OllamaLocalExecutor.buildUrl(...,null)==="http://localhost:11434/api/chat".
    - File is self-contained (no network, no vi.mock, no new deps).
    - Phase 1/2/3 tests still green (regression): `cd tests && npx vitest run unit/ollama-claude-transport.test.js unit/ollama-claude-block-fidelity.test.js unit/ollama-claude-compat.test.js` exits 0.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: VAL-02 mocked /v1/messages SSE round-trip test</name>
  <files>tests/unit/ollama-claude-round-trip.test.js</files>
  <read_first>
    - tests/translator/golden-response-stream.test.js (THE analog — runStream accumulator pattern lines 24-33, chunk array shape lines 37-50)
    - open-sse/translator/index.js lines 149-189 (translateResponse — line 152 `if (sourceFormat === targetFormat) return [chunk]` identity passthrough is the load-bearing property)
    - open-sse/translator/formats.js (FORMATS.CLAUDE constant)
    - .planning/phases/04-validation-tests/04-PATTERNS.md (VAL-02 mock event array + runStream helper + assertion block)
    - .planning/PROJECT.md (Ollama /v1/messages SSE event shape: message_start, content_block_start/delta with text_delta/thinking_delta/input_json_delta, content_block_stop, message_delta with stop_reason+usage, message_stop)
  </read_first>
  <behavior>
    - Contract VAL-02: a recorded /v1/messages SSE chunk array (message_start with usage → content_block_start/delta for thinking (thinking_delta) → content_block_stop → content_block_start/delta for text (text_delta) → content_block_stop → content_block_start/delta for tool_use (input_json_delta) → content_block_stop → message_delta with stop_reason:"tool_use" + usage.output_tokens → message_stop) fed through translateResponse(FORMATS.CLAUDE, FORMATS.CLAUDE, chunk, state) with a runStream accumulator produces an output array where:
      - The output type sequence equals the input type sequence (no events dropped, no reordering): out.map(e=>e.type).toEqual(events.map(e=>e.type)).
      - Each output event is the SAME reference as the input event (translateResponse returns [chunk] on same-format — identity, not a copy): expect(out[i]).toBe(events[i]).
      - thinking_delta survives: out.find(e=>e.delta?.type==="thinking_delta").delta.thinking === "let me think".
      - tool_use block survives: out.find(e=>e.content_block?.type==="tool_use").content_block.id === "toolu_1" AND .name === "get_weather".
      - input_json_delta survives verbatim: out.find(e=>e.delta?.type==="input_json_delta").delta.partial_json === '{"city":"NYC"}'.
      - stop_reason reaches the client: out.find(e=>e.type==="message_delta").delta.stop_reason === "tool_use".
      - usage reaches the client: out.find(e=>e.type==="message_delta").usage.output_tokens === 5.
  </behavior>
  <action>
    Create tests/unit/ollama-claude-round-trip.test.js. Use the golden-response-stream.test.js harness pattern: import { describe, it, expect } from "vitest"; import { translateResponse, initState } from "../../open-sse/translator/index.js"; import { FORMATS } from "../../open-sse/translator/formats.js".

    Do NOT import registerAll.js (CLAUDE→CLAUDE is identity passthrough at index.js:152, no translator registry needed — confirm by the test passing without the import).

    Define runStream(targetFormat, sourceFormat, events) helper copying the analog: const state = initState(sourceFormat); const all = []; for (const ev of events) { const out = translateResponse(targetFormat, sourceFormat, ev, state); if (Array.isArray(out)) all.push(...out); else if (out) all.push(out); } return all. Do NOT call stripVolatile (the analog strips volatile ids for snapshot stability; VAL-02 uses explicit assertions, not snapshots, so identity references must be preserved — stripping would break the toBe identity check).

    describe("Phase 4: VAL-02 round-trip (mocked /v1/messages)"). One it block "Contract VAL-02: thinking + tool_use survive end-to-end through claude passthrough (mocked /v1/messages)".

    Build the events array as a recorded /v1/messages SSE sequence per PROJECT.md's documented event shape — 12 events: message_start (message:{id:"msg_1",model:"claude-sonnet-4-5",usage:{input_tokens:10}}) → content_block_start index:0 content_block:{type:"thinking"} → content_block_delta index:0 delta:{type:"thinking_delta",thinking:"let me think"} → content_block_stop index:0 → content_block_start index:1 content_block:{type:"text"} → content_block_delta index:1 delta:{type:"text_delta",text:"Answer"} → content_block_stop index:1 → content_block_start index:2 content_block:{type:"tool_use",id:"toolu_1",name:"get_weather"} → content_block_delta index:2 delta:{type:"input_json_delta",partial_json:'{"city":"NYC"}'} → content_block_stop index:2 → message_delta delta:{stop_reason:"tool_use"} usage:{output_tokens:5} → message_stop.

    const out = runStream(FORMATS.CLAUDE, FORMATS.CLAUDE, events). Assert per the behavior block: type sequence equality, reference identity (out[i]===events[i]), thinking_delta content, tool_use id+name, input_json_delta partial_json, stop_reason, usage.output_tokens.

    Add a comment above runStream: "VAL-02 mock = inline recorded chunk array, NOT a fetch mock (CONTEXT.md line 47). No network, no vi.mock."

    No try/catch. No fenced code in this action.
  </action>
  <verify>
    <automated>cd tests && npx vitest run unit/ollama-claude-round-trip.test.js</automated>
  </verify>
  <done>
    - `cd tests && npx vitest run unit/ollama-claude-round-trip.test.js` exits 0.
    - runStream CLAUDE→CLAUDE over the 12-event recorded sequence produces 12 output events with identical type order.
    - Each output event is reference-identical to the input (translateResponse returns [chunk] on same-format).
    - thinking_delta surfaces as thinking content ("let me think"); tool_use block surfaces with id "toolu_1" + name "get_weather"; input_json_delta partial_json '{"city":"NYC"}' survives verbatim.
    - stop_reason "tool_use" + usage.output_tokens 5 reach the client-facing stream.
    - File is self-contained (no network, no vi.mock, no registerAll import, no new deps).
    - No open-sse source files modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-file → open-sse imports | Pure-function test imports cross no trust boundary — no untrusted input, no network, no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | test fixture chunk arrays | low | accept | Inline literal fixtures, no external input — no tampering surface |
| T-04-02 | Information Disclosure | test file hardcodes no secrets | low | accept | Fixtures use synthetic ids (msg_1, toolu_1), no real credentials/keys |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | Phase 4 adds zero new deps — test-only, no install tasks; slopcheck N/A |

Lowest-risk phase: pure tests, no auth/attack-surface change. No high-severity threats to mitigate.
</threat_model>

<verification>
- `cd tests && npx vitest run unit/ollama-claude-regression.test.js` exits 0 (VAL-01 + VAL-03).
- `cd tests && npx vitest run unit/ollama-claude-round-trip.test.js` exits 0 (VAL-02).
- Regression: `cd tests && npx vitest run unit/ollama-claude-transport.test.js unit/ollama-claude-block-fidelity.test.js unit/ollama-claude-compat.test.js` still exits 0.
- No open-sse/ source files modified (git diff --stat open-sse/ is empty).
- No new dependencies added (package.json unchanged).
</verification>

<success_criteria>
- VAL-01 locked: regression test asserts claude-format → targetFormat="claude" + body structurally identical (no openai hop) for ollama + ollama-local.
- VAL-02 locked: mocked /v1/messages round-trip confirms thinking + tool_use survive end-to-end (12-event SSE sequence, identity passthrough, order preserved).
- VAL-03 locked: guard test — non-Claude openai-format → /api/chat unchanged (resolveTransport null + buildUrl fallback).
- All three test files green on plain checkout; Phase 1/2/3 tests still green.
</success_criteria>

<output>
Create `.planning/phases/04-validation-tests/04-01-SUMMARY.md` when done.
</output>