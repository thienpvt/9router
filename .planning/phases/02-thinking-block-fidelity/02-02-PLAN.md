---
phase: 02-thinking-block-fidelity
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/ollama-claude-block-fidelity.test.js
autonomous: true
requirements: [BLK-01, BLK-02, BLK-03, BLK-04]
tags: [ollama, claude, blocks, passthrough, vitest]

must_haves:
  truths:
    - "tool_use and tool_result content blocks round-trip losslessly through the same-format claude→claude passthrough for ollama (ids, names, JSON input preserved)"
    - "text content blocks and system (string or array) pass through translateRequest unchanged for ollama+claude (modulo cache_control rewrite which is Phase 3 COMP-01)"
    - "base64 image content blocks pass through translateRequest unchanged for ollama+claude"
    - "same-format response passthrough (translateResponse claude→claude) returns [chunk] unchanged — the SSE reconstruction contract"
  artifacts:
    - "tests/unit/ollama-claude-block-fidelity.test.js (runnable self-check covering BLK-01/02/03/04)"
  key_links:
    - "translateRequest same-format skip (index.js:78) → tool_use/tool_result/text/image blocks pass untouched"
    - "translateResponse same-format passthrough (index.js:152-153) → ollama native SSE forwarded unchanged (BLK-04 reconstruction contract)"
---

<objective>
Verify the existing same-format passthrough leaves Claude-native content blocks (tool_use, tool_result, text, system, base64 image) untouched for ollama, and assert the same-format response passthrough returns chunks unchanged — the BLK-01/02/03/04 contract. Test-only; no code change unless a test reveals the passthrough mangles something.

Purpose: BLK-01/02/03 — the same-format skip (index.js:78) + prepareClaudeRequest (cache_control only for ollama, thinking-block rewrite gated off) should leave Claude-native blocks untouched. BLK-04 — translateResponse same-format returns [chunk] (index.js:152-153), forwarding ollama's native SSE sequence unchanged. This plan proves that contract holds.
Output: A runnable vitest self-check covering all four BLK contracts. If any test fails, scope the fix here (CONTEXT: "If a test reveals prepareClaudeRequest mangles something for ollama, scope that fix here").
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-thinking-block-fidelity/02-CONTEXT.md
@.planning/phases/02-thinking-block-fidelity/02-PATTERNS.md
@.planning/phases/01-passthrough-transport-auth/01-02-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED→GREEN): block fidelity self-check — tool_use/tool_result/text/system/image round-trip + SSE passthrough</name>
  <files>tests/unit/ollama-claude-block-fidelity.test.js</files>
  <read_first>
    - tests/unit/ollama-claude-transport.test.js (Phase 1 harness analog — vitest, no mocks, direct imports, Contract naming)
    - open-sse/translator/index.js (lines 70-107 translateRequest — same-format skip at line 78; lines 148-160 translateResponse — same-format returns [chunk] at line 152-153)
    - open-sse/translator/formats.js (FORMATS.CLAUDE constant line 6 — use this, do not hardcode "claude")
    - open-sse/translator/formats/claude.js (prepareClaudeRequest — confirm cache_control rewrite runs for ollama but content blocks themselves are untouched; handlesThinkingBlocks gate at line 87 excludes ollama)
    - open-sse/translator/concerns/thinkingUnified.js (applyThinking — may or may not have the 02-01 short-circuit yet; this plan must not depend on 02-01 being complete. Use a body WITHOUT thinking fields so applyThinking is a no-op regardless of whether the short-circuit exists. This makes 02-02 independent of 02-01 and allows both to run in parallel Wave 1.)
  </read_first>
  <behavior>
    - Contract BLK-01: claude-shape body with messages:[{role:assistant,content:[{type:"tool_use",id:"toolu_1",name:"get_weather",input:{city:"SF"}}]},{role:user,content:[{type:"tool_result",tool_use_id:"toolu_1",content:"72F"}]}] → after translateRequest(FORMATS.CLAUDE,FORMATS.CLAUDE,model,body,{stream:false},null,null,"ollama"), the assistant tool_use block (id, name, input) and the user tool_result block (tool_use_id, content) are present and unchanged in result.messages
    - Contract BLK-02: body with system:"You are helpful" + messages:[{role:user,content:[{type:"text",text:"hello"}]}] → after translateRequest(...,"ollama"), result.system === "You are helpful" (or equivalent array form with same content) and the text block text === "hello" in result.messages. Assert content value, NOT byte-identity (cache_control may be added by prepareClaudeRequest — Phase 3 COMP-01 concern).
    - Contract BLK-03: body with messages:[{role:user,content:[{type:"image",source:{type:"base64",media_type:"image/png",data:"iVBORw0KGgo="}}]}] → after translateRequest(...,"ollama"), the image block (type, source.type, source.media_type, source.data) is present and unchanged in result.messages
    - Contract BLK-04: translateResponse(FORMATS.CLAUDE,FORMATS.CLAUDE, {type:"message_start",message:{id:"msg_1"}}, {}) returns an array of length 1 containing the chunk unchanged — the SSE reconstruction contract (ollama native sequence forwarded verbatim)
  </behavior>
  <action>
    Create tests/unit/ollama-claude-block-fidelity.test.js following the Phase 1 test harness pattern (tests/unit/ollama-claude-transport.test.js): vitest describe/it, no mocks, direct imports. Import translateRequest and translateResponse from ../../open-sse/translator/index.js and FORMATS from ../../open-sse/translator/formats.js. describe block title: "Phase 2: Block fidelity for ollama claude passthrough". Four it blocks named per the behavior contracts above. CRITICAL for independence from 02-01: construct test bodies WITHOUT a thinking field or output_config so applyThinking is a no-op regardless of whether the 02-01 short-circuit exists yet (applyThinking only strips thinking-related fields; tool_use/tool_result/text/system/image blocks are never touched by stripAll — verify this in the read_first pass). This ensures both plans run in parallel Wave 1 without ordering dependency. For BLK-04, pass a simple chunk object and assert the return is [chunk] (array, length 1, first element === the input chunk). Run the tests — these should pass immediately since the same-format passthrough already exists (index.js:78 skip + index.js:152-153 passthrough). If all pass, this is GREEN-on-first-run (the existing code already satisfies the contract — this test LOCKS the contract against future refactors). If any test fails, investigate whether prepareClaudeRequest mangled the block and scope the minimal fix here. Commit: test(02-02): add block fidelity self-check for ollama passthrough. Per CLAUDE.md lazy-senior rule this is the ONE runnable self-check for the block-fidelity verification.
  </action>
  <verify>
    <automated>cd tests && npx vitest run unit/ollama-claude-block-fidelity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File tests/unit/ollama-claude-block-fidelity.test.js exists with exactly 4 it blocks named "Contract BLK-01", "Contract BLK-02", "Contract BLK-03", "Contract BLK-04"
    - Imports use FORMATS.CLAUDE (not the literal string "claude") for sourceFormat/targetFormat arguments
    - No beforeEach/setup blocks; pure function in/out assertions
    - Test bodies contain NO thinking field or output_config (independence from 02-01)
    - Test run: cd tests && npx vitest run unit/ollama-claude-block-fidelity.test.js → 4 passed (0 failed). If a test revealed a real mangling, a minimal fix was applied and documented; if no mangling, the test locks the existing contract.
    - Commit exists with message starting "test(02-02):"
    - Existing tests still pass: cd tests && npx vitest run unit/ollama-claude-transport.test.js → 7 passed (0 failed)
  </acceptance_criteria>
  <done>4-contract block-fidelity self-check committed and passing; Phase 1 transport tests still pass (7/7); commit exists with message starting test(02-02):</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→gateway | Claude-format request with content blocks (tool_use/tool_result/text/image) — untrusted input crosses here |
| gateway→ollama | Gateway forwards body to ollama /v1/messages — ollama already authed via Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | content block passthrough (test-only plan) | low | accept | No code change in this plan (test-only). Verifies existing passthrough does not mangle blocks — no new attack surface. |
| T-02-04 | Information Disclosure | tool_result content forwarded to ollama | low | accept | tool_result content is client-originated data the client explicitly sent; forwarding verbatim is the intended contract (BLK-01). No gateway-originated secrets in content blocks. |
| T-02-SC2 | Tampering | npm install (none this phase) | low | accept | No new dependencies — test-only plan. No package legitimacy gate needed. |
</threat_model>

<verification>
- cd tests && npx vitest run unit/ollama-claude-block-fidelity.test.js → 4 passed
- cd tests && npx vitest run unit/ollama-claude-transport.test.js → 7 passed (no regression)
- If a minimal fix was needed, grep confirms the fix is scoped to the specific mangling (not a broad prepareClaudeRequest rewrite)
</verification>

<success_criteria>
- tool_use/tool_result blocks round-trip losslessly (BLK-01)
- text + system blocks pass through with content preserved (BLK-02)
- base64 image blocks pass through unchanged (BLK-03)
- same-format response passthrough returns [chunk] unchanged (BLK-04 reconstruction contract)
- Existing Phase 1 transport tests still pass (no regression)
</success_criteria>

<output>
Create .planning/phases/02-thinking-block-fidelity/02-02-SUMMARY.md when done

## Artifacts this phase produces

- tests/unit/ollama-claude-block-fidelity.test.js — 4-contract runnable self-check (BLK-01/02/03/04) locking the passthrough block-fidelity contract against future refactors
</output>