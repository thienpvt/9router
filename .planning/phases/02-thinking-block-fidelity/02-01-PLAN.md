---
phase: 02-thinking-block-fidelity
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - open-sse/translator/concerns/thinkingUnified.js
  - tests/unit/ollama-claude-thinking-passthrough.test.js
autonomous: true
requirements: [THINK-01, THINK-02, THINK-03]
tags: [ollama, claude, thinking, applyThinking, vitest]

must_haves:
  truths:
    - "applyThinking returns body unchanged when targetFormat is claude and provider is ollama (thinking field + output_config.effort survive)"
    - "applyThinking still strips thinking fields for non-ollama non-reasoning providers (regression guard)"
    - "providerThinking-injected thinking field survives translateRequest for ollama+claude path"
    - "thinking content blocks in assistant messages survive translateRequest for ollama+claude path"
  artifacts:
    - "open-sse/translator/concerns/thinkingUnified.js (applyThinking short-circuit added)"
    - "tests/unit/ollama-claude-thinking-passthrough.test.js (runnable self-check)"
  key_links:
    - "applyThinking ollama-claude early-return → prevents stripAll from deleting output_config.effort + thinking"
    - "short-circuit placed before the !caps.reasoning branch (thinkingUnified.js line 277) so ollama wins despite no reasoning capability entry"
---

<objective>
Make applyThinking a no-op on the claude→ollama path so client thinking fields and output_config.effort reach ollama unchanged, and prove providerThinking injection + thinking blocks survive translateRequest.

Purpose: THINK-02 — today applyThinking calls stripAll when !caps.reasoning, deleting the client's thinking field and output_config.effort before they reach ollama. Ollama accepts thinking blocks natively; the short-circuit prevents the strip. THINK-01 request-side: thinking content blocks in assistant messages pass verbatim. THINK-03: providerThinking injection (chatCore.js:59-69) survives because applyThinking no longer strips it.
Output: Modified thinkingUnified.js with the ollama-claude early-return + a runnable vitest self-check proving the contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-thinking-block-fidelity/02-CONTEXT.md
@.planning/phases/02-thinking-block-fidelity/02-PATTERNS.md
@.planning/phases/01-passthrough-transport-auth/01-02-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): thinking passthrough self-check — applyThinking ollama-claude no-op + providerThinking survival</name>
  <files>tests/unit/ollama-claude-thinking-passthrough.test.js</files>
  <read_first>
    - tests/unit/ollama-claude-transport.test.js (Phase 1 harness analog — vitest, no mocks, direct imports, Contract naming)
    - open-sse/translator/concerns/thinkingUnified.js (full file — applyThinking at line 269, stripAll at line 164, the !caps.reasoning branch at line 277)
    - open-sse/translator/formats.js (FORMATS.CLAUDE constant at line 6 — use this, do not hardcode the string "claude")
    - open-sse/translator/index.js (lines 70-107 — translateRequest same-format skip at line 78, applyThinking call at line 107, prepareClaudeRequest at line 118)
    - open-sse/handlers/chatCore.js (lines 59-69 — providerThinking injection shape: body.thinking or body.reasoning_effort)
  </read_first>
  <behavior>
    - Contract THINK-02: applyThinking("claude", "ollama-model", {thinking:{type:"enabled"}, output_config:{effort:"high"}}, "ollama") returns body with thinking.type === "enabled" and output_config.effort === "high" (unchanged)
    - Contract THINK-02 negative: applyThinking("claude", "some-model", {thinking:{type:"enabled"}, output_config:{effort:"high"}}, "nonexistent-provider") returns body with thinking and output_config deleted (stripAll ran — regression guard that short-circuit is ollama-specific)
    - Contract THINK-03: body shaped like providerThinking injection output {thinking:{type:"enabled",budget_tokens:10000}} → after translateRequest("claude","claude", model, body, {stream:false}, null, null, "ollama"), result.thinking.type === "enabled" (injection survived)
    - Contract THINK-01 request: assistant message with thinking content block [{type:"thinking",thinking:"...",signature:"..."}] → after translateRequest("claude","claude", model, body, ..., "ollama"), the thinking block is present in result.messages unchanged
  </behavior>
  <action>
    Create tests/unit/ollama-claude-thinking-passthrough.test.js following the Phase 1 test harness pattern (tests/unit/ollama-claude-transport.test.js): vitest describe/it, no mocks, direct imports. Import applyThinking from ../../open-sse/translator/concerns/thinkingUnified.js and translateRequest from ../../open-sse/translator/index.js. Use FORMATS.CLAUDE from ../../open-sse/translator/formats.js for the targetFormat argument (per CLAUDE.md: no hardcoded role/block/model strings). describe block title: "Phase 2: Thinking passthrough for ollama claude transport". Four it blocks named per the behavior contracts above. For the THINK-03 + THINK-01 request contracts, call translateRequest with sourceFormat=FORMATS.CLAUDE, targetFormat=FORMATS.CLAUDE, provider="ollama" to exercise the same-format skip + applyThinking + prepareClaudeRequest pipeline. Run the tests — THINK-02 and THINK-02 negative will both FAIL (applyThinking strips for ollama today because caps.reasoning is false and there is no short-circuit). THINK-03 and THINK-01 request may also fail for the same reason (stripAll deletes the thinking field before it reaches prepareClaudeRequest). Commit RED: test(02-01): add thinking passthrough self-check. Per CLAUDE.md lazy-senior rule this is the ONE runnable self-check for the applyThinking short-circuit logic.
  </action>
  <verify>
    <automated>cd tests && npx vitest run unit/ollama-claude-thinking-passthrough.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File tests/unit/ollama-claude-thinking-passthrough.test.js exists with exactly 4 it blocks named "Contract THINK-02", "Contract THINK-02 negative", "Contract THINK-03", "Contract THINK-01 request"
    - Imports use FORMATS.CLAUDE (not the literal string "claude") for targetFormat arguments
    - No beforeEach/setup blocks; pure function in/out assertions
    - Test run shows at least Contract THINK-02 FAILING (asserting thinking field deleted when it should survive) — RED confirmed before implementation
    - RED commit exists with message starting "test(02-01):"
  </acceptance_criteria>
  <done>4-contract self-check file committed; at least Contract THINK-02 confirmed RED (failing before implementation); RED commit exists with message starting test(02-01):</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): add applyThinking ollama-claude early-return short-circuit</name>
  <files>open-sse/translator/concerns/thinkingUnified.js</files>
  <read_first>
    - open-sse/translator/concerns/thinkingUnified.js (applyThinking function lines 269-287; stripAll lines 164-174; the !caps.reasoning branch at line 277 — the short-circuit must be placed BEFORE this branch)
    - open-sse/translator/formats.js (FORMATS.CLAUDE constant line 6 — import and use it, do not hardcode "claude")
  </read_first>
  <behavior>
    - applyThinking("claude", anyModel, anyBody, "ollama") returns anyBody unchanged (thinking + output_config.effort + all fields preserved)
    - applyThinking for non-ollama providers still calls stripAll when !caps.reasoning (existing branch unchanged)
  </behavior>
  <action>
    In open-sse/translator/concerns/thinkingUnified.js, add an import of FORMATS from ../../translator/formats.js (if not already imported — check existing imports at top of file; FORMATS may need a new import line). In the applyThinking function body, insert an early-return between the existing guard (line 270: if (!body || typeof body !== "object") return body) and the !caps.reasoning branch (line 277). The early-return: if provider === "ollama" and targetFormat === FORMATS.CLAUDE, return body unchanged. Add a ponytail comment on the line naming the ceiling and upgrade path: ceiling is ollama under the claude transport; lift into PROVIDERS[ollama].quirks or a capability flag if a second native-claude provider lands. Do NOT add ollama to handlesThinkingBlocks in claude.js (CONTEXT decision — that gate is for assistant-message thinking-block rewrites, a different concern). Do NOT tag ollama models with a reasoning capability (CONTEXT chose the short-circuit instead). Run the self-check from Task 1 — all 4 contracts must now pass. Commit GREEN: feat(02-01): short-circuit applyThinking for ollama claude path.
  </action>
  <verify>
    <automated>cd tests && npx vitest run unit/ollama-claude-thinking-passthrough.test.js</automated>
  </verify>
  <acceptance_criteria>
    - open-sse/translator/concerns/thinkingUnified.js applyThinking contains an early-return matching provider === "ollama" and targetFormat === FORMATS.CLAUDE that returns body unchanged, placed before the !caps.reasoning branch
    - The short-circuit uses FORMATS.CLAUDE constant (no hardcoded string "claude")
    - A ponytail comment names the ceiling (ollama under claude transport) and upgrade path (lift to quirks/capability flag if second native-claude provider lands)
    - handlesThinkingBlocks in claude.js is NOT modified (grep confirms ollama not added)
    - Test run: cd tests && npx vitest run unit/ollama-claude-thinking-passthrough.test.js → 4 passed (0 failed)
    - GREEN commit exists with message starting "feat(02-01):"
    - Existing tests still pass: cd tests && npx vitest run unit/ollama-claude-transport.test.js → 7 passed (0 failed)
  </acceptance_criteria>
  <done>applyThinking ollama-claude early-return committed; all 4 thinking-passthrough contracts pass; Phase 1 transport tests still pass (7/7); GREEN commit exists with message starting feat(02-01):</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→gateway | Claude-format request enters with thinking fields + output_config.effort — untrusted input crosses here |
| gateway→ollama | Gateway forwards body to ollama /v1/messages — ollama already authed via Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | applyThinking short-circuit | low | accept | Short-circuit returns body unchanged — no mutation, no new input parsing. Ollama already behind Phase 1 auth. No new attack surface. |
| T-02-02 | Information Disclosure | thinking field forwarded to ollama | low | accept | Thinking blocks are client-originated content the client explicitly sent; forwarding verbatim is the intended contract (THINK-01). No gateway-originated secrets in body. |
| T-02-SC | Tampering | npm install (none this phase) | low | accept | No new dependencies introduced — pure JS early-return. No package legitimacy gate needed. |
</threat_model>

<verification>
- cd tests && npx vitest run unit/ollama-claude-thinking-passthrough.test.js → 4 passed
- cd tests && npx vitest run unit/ollama-claude-transport.test.js → 7 passed (no regression)
- grep -n "ollama" open-sse/translator/concerns/thinkingUnified.js confirms the short-circuit line exists
- grep -n "handlesThinkingBlocks" open-sse/translator/formats/claude.js confirms ollama was NOT added
</verification>

<success_criteria>
- applyThinking is a no-op for targetFormat=claude + provider=ollama (THINK-02)
- providerThinking-injected thinking survives translateRequest for ollama+claude (THINK-03)
- thinking content blocks in assistant messages survive translateRequest for ollama+claude (THINK-01 request-side)
- Non-ollama non-reasoning providers still get stripAll (regression guard)
- Existing Phase 1 transport tests still pass (no regression)
</success_criteria>

<output>
Create .planning/phases/02-thinking-block-fidelity/02-01-SUMMARY.md when done

## Artifacts this phase produces

- open-sse/translator/concerns/thinkingUnified.js — applyThinking ollama-claude early-return short-circuit (the ONE behavioral code change in Phase 2)
- tests/unit/ollama-claude-thinking-passthrough.test.js — 4-contract runnable self-check (THINK-01 request, THINK-02, THINK-03)
</output>