---
phase: 03-compatibility-fallback
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/ollama-claude-compat.test.js
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]
user_setup: []

must_haves:
  truths:
    # COMP-01 — ignored fields tolerated (sent as-is, ollama ignores, no gateway stripping)
    - "tool_choice survives translateRequest(claude→claude, provider=ollama) to the dispatched body when tools array is non-empty"
    - "metadata survives translateRequest(claude→claude, provider=ollama) to the dispatched body"
    - "cache_control on content blocks is rewritten by prepareClaudeRequest to the ephemeral form (not stripped) for ollama"
    # COMP-02 — stop_reason passthrough (same-format returns [chunk] unchanged)
    - "ollama message_delta with delta.stop_reason='tool_use' passes through translateResponse(claude→claude) unchanged"
    - "ollama message_delta with delta.stop_reason='end_turn' passes through translateResponse(claude→claude) unchanged"
    - "ollama message_delta with delta.stop_reason='max_tokens' passes through translateResponse(claude→claude) unchanged"
    # COMP-03 — usage extraction (Claude-shape aware, provider-agnostic)
    - "message_start chunk with message.usage.input_tokens=N yields prompt_tokens=N via extractUsage"
    - "message_delta chunk with usage.output_tokens=M yields completion_tokens=M via extractUsage"
    - "mergeUsage(extractUsage(start), extractUsage(delta)) preserves input_tokens from start and output_tokens from delta"
    # Non-Claude fallback guard (re-locks PASS-03)
    - "resolveTransport(ollama, openai) === null — openai source stays on default ollama /api/chat transport"
    - "resolveTransport(ollama-local, openai) === null — local openai fallback preserved"
  artifacts:
    - "tests/unit/ollama-claude-compat.test.js — one runnable vitest self-check covering COMP-01/02/03 + fallback guard"
  key_links:
    - "translateRequest same-format skip (open-sse/translator/index.js:78) preserves tool_choice/metadata/cache_control for ollama"
    - "translateResponse same-format passthrough (open-sse/translator/index.js:152-153) forwards stop_reason unchanged"
    - "extractUsage Claude-shape branches (open-sse/utils/usageTracking.js:239-257) read message_start.message.usage + message_delta.usage"
---

<objective>
Phase 3 compatibility & fallback verification. Prove the existing same-format passthrough + generic usage extraction deliver COMP-01/02/03 for ollama's Claude-native values, and lock the non-Claude fallback path with a guard test. Test-only — no source change expected unless COMP-03 reveals a usage-extraction gap (PATTERNS.md confirms existing branches handle Claude shape).

Purpose: Lock the compatibility contract so future refactors can't silently strip ignored fields, remap stop_reason, or break usage tracking on the ollama claude path.
Output: One vitest self-check (tests/unit/ollama-claude-compat.test.js) — 4 contract groups (COMP-01 field tolerance, COMP-02 stop_reason passthrough, COMP-03 usage extraction, fallback guard).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compatibility-fallback/03-CONTEXT.md
@.planning/phases/03-compatibility-fallback/03-PATTERNS.md
@.planning/phases/02-thinking-block-fidelity/02-02-SUMMARY.md
@.planning/phases/01-passthrough-transport-auth/01-02-SUMMARY.md

@open-sse/translator/index.js
@open-sse/translator/formats.js
@open-sse/translator/formats/claude.js
@open-sse/services/provider.js
@open-sse/utils/usageTracking.js
@tests/unit/ollama-claude-transport.test.js
@tests/unit/ollama-claude-block-fidelity.test.js
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD RED→GREEN — ollama-claude compat self-check (COMP-01/02/03 + fallback guard)</name>
  <files>tests/unit/ollama-claude-compat.test.js</files>
  <read_first>
    - tests/unit/ollama-claude-block-fidelity.test.js (exact harness template: imports, describe/it Contract naming, translateRequest/translateResponse call shape)
    - tests/unit/ollama-claude-transport.test.js (fallback guard analog: resolveTransport null assertions)
    - open-sse/translator/index.js lines 78, 152-153 (same-format skip + passthrough — the mechanism COMP-01/02 exercise)
    - open-sse/translator/formats/claude.js lines 220-360 (prepareClaudeRequest cache_control rewrite + tool_choice delete-only-when-tools-empty — confirms tolerate-don't-strip)
    - open-sse/utils/usageTracking.js lines 233-329 (extractUsage Claude-shape branches + mergeUsage field-wise max — COMP-03 entry points)
    - open-sse/services/provider.js lines 128-147 (getTargetFormat, resolveTransport — fallback guard)
  </read_first>
  <behavior>
    COMP-01 field tolerance:
      - translateRequest(CLAUDE, CLAUDE, model, body-with-tool_choice-and-tools, false, null, "ollama").tool_choice === body.tool_choice (preserved when tools non-empty)
      - translateRequest(..., body-with-metadata, ...).metadata deep-equals the input metadata
      - A content block with cache_control:{type:"ephemeral"} on a system block survives to the dispatched body in the rewritten ephemeral form (prepareClaudeRequest rewrites, doesn't strip)
    COMP-02 stop_reason passthrough:
      - translateResponse(CLAUDE, CLAUDE, {type:"message_delta", delta:{stop_reason:"tool_use"}}, {}) returns [chunk] with result[0].delta.stop_reason === "tool_use"
      - Same for "end_turn" and "max_tokens"
    COMP-03 usage extraction:
      - extractUsage({type:"message_start", message:{usage:{input_tokens:100}}}).prompt_tokens === 100
      - extractUsage({type:"message_delta", usage:{output_tokens:50}}).completion_tokens === 50
      - mergeUsage(extractUsage(start), extractUsage(delta)).prompt_tokens === 100 AND .completion_tokens === 50
    Fallback guard:
      - resolveTransport("ollama","openai") === null
      - resolveTransport("ollama-local","openai") === null
  </behavior>
  <action>
RED: Create tests/unit/ollama-claude-compat.test.js per the Phase 2 harness pattern (direct imports from ../../open-sse/..., describe("Phase 3: ...", ...), it("Contract COMP-XX: ...", ...)). Import: describe/expect/it from vitest; translateRequest/translateResponse from ../../open-sse/translator/index.js; FORMATS from ../../open-sse/translator/formats.js; resolveTransport from ../../open-sse/services/provider.js; extractUsage/mergeUsage from ../../open-sse/utils/usageTracking.js. No mocks — synthetic chunks + direct module calls only.

Write 4 contract groups (per D-01 tolerate-don't-strip, D-02 no-normalizer, D-03 generic-extraction, D-04 fallback-preserved from 03-CONTEXT.md):

Contract group COMP-01 (field tolerance): Build a claude body with non-empty tools array + tool_choice:{type:"auto"} + metadata:{user_id:"u1"}. Call translateRequest(FORMATS.CLAUDE, FORMATS.CLAUDE, model, body, false, null, "ollama"). Assert result.tool_choice equals the input object, result.metadata deep-equals input metadata. Add a system block with cache_control:{type:"ephemeral"} on the last system array entry; assert the dispatched result's system block carries the rewritten cache_control (type:"ephemeral", ttl:"1h" — the prepareClaudeRequest rewrite, not stripped). Use FORMATS.CLAUDE constant — do not hardcode "claude".

Contract group COMP-02 (stop_reason passthrough): Three it blocks — one each for stop_reason "tool_use", "end_turn", "max_tokens". Each constructs a message_delta chunk {type:"message_delta", delta:{stop_reason:VALUE}, usage:{output_tokens:1}} and calls translateResponse(FORMATS.CLAUDE, FORMATS.CLAUDE, chunk, {}). Assert Array.isArray(result), result.length===1, result[0]===chunk (same identity — same-format returns [chunk] at index.js:152-153). No normalizer involved.

Contract group COMP-03 (usage extraction): Two it blocks. First: const start = {type:"message_start", message:{usage:{input_tokens:100}}}; const u1 = extractUsage(start); expect(u1.prompt_tokens).toBe(100). Second: const delta = {type:"message_delta", usage:{output_tokens:50}}; const u2 = extractUsage(delta); expect(u2.completion_tokens).toBe(50); const merged = mergeUsage(u1, u2); expect(merged.prompt_tokens).toBe(100); expect(merged.completion_tokens).toBe(50). Optionally add a cache_read_input_tokens assertion if the chunk carries it (proves ollama cache fields flow through the same branch).

Contract group Fallback guard: Two it blocks re-asserting PASS-03 — resolveTransport("ollama","openai")===null and resolveTransport("ollama-local","openai")===null (mirrors tests/unit/ollama-claude-transport.test.js:20-26). Optionally assert getTargetFormat("ollama") yields "ollama" so the non-Claude path routes /api/chat — but only if getTargetFormat is importable without side effects; skip if it requires provider init.

Run RED: `cd tests && npx vitest run unit/ollama-claude-compat.test.js` — expect pass on the assertions that exercise existing passthrough behavior (COMP-01/02/03 describe what the code already does; only the test file is new). If all pass on first run, that IS the green — the test locks the contract. Commit RED-or-GREEN accordingly: if RED (any fail), commit as test(03-01) RED; then fix the test or source per the gap; commit GREEN. If all pass immediately, single commit test(03-01): add Phase 3 compat self-check (the test is both the spec and the lock).

GREEN: If any contract fails, the plan authorizes ONE minimal source fix per 03-CONTEXT.md integration point: "If a test reveals a usage-extraction gap (e.g. usage shape mismatch), scope a minimal fix." The likely gap is none — PATTERNS.md confirms open-sse/utils/usageTracking.js:239-257 already reads message_start.message.usage.input_tokens + message_delta.usage.output_tokens (Claude shape, provider-agnostic). If a gap appears, fix is 1-3 lines in the existing Claude-shape branch of usageTracking.js or sseToJsonHandler.js — do NOT add an ollama-specific code path or a stop_reason normalizer. If no gap, no source change; the test file is the only artifact.

Do NOT:
- Add a stop_reason normalizer for ollama (CONTEXT D-02: ollama emits Claude-native values, no mapping).
- Strip tool_choice/cache_control/metadata for ollama (CONTEXT D-01: tolerate, don't strip).
- Add a new translator (CONTEXT: same-format skip is the mechanism).
- Modify sseToJsonHandler.js unless COMP-03 test proves a gap (PATTERNS.md: lines 125-127/146-147 already handle Claude shape).
  </action>
  <verify>
    <automated>cd tests && npx vitest run unit/ollama-claude-compat.test.js</automated>
  </verify>
  <done>
    - tests/unit/ollama-claude-compat.test.js exists and `cd tests && npx vitest run unit/ollama-claude-compat.test.js` exits 0 (all contract blocks green)
    - File imports use FORMATS.CLAUDE (no hardcoded "claude"/"openai" strings) per CLAUDE.md open-sse/AGENTS.md convention
    - 4 contract groups present: COMP-01 (field tolerance: tool_choice + metadata + cache_control rewrite), COMP-02 (3 stop_reason values: tool_use/end_turn/max_tokens), COMP-03 (message_start input + message_delta output + merge), Fallback guard (resolveTransport null for openai on ollama + ollama-local)
    - No regression: `cd tests && npx vitest run unit/ollama-claude-transport.test.js unit/ollama-claude-block-fidelity.test.js` still green (Phase 1/2 contracts intact)
    - If source was modified, the diff is 1-3 lines in an existing Claude-shape branch (no new code path, no normalizer, no ollama-specific branch)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→module | Test file imports open-sse modules directly; no untrusted input crosses a trust boundary in this phase (synthetic chunks only, no live provider calls, no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | tests/unit/ollama-claude-compat.test.js | low | accept | Test-only file; no production attack surface. Synthetic chunks, no live calls, no credentials. Phase verifies existing behavior, introduces no new trust boundary. |
| T-03-02 | Information Disclosure | open-sse/utils/usageTracking.js extractUsage | low | accept | COMP-03 test feeds synthetic usage tokens; no real user data. Existing extraction path already Claude-shape-aware (Phase 1/2 precedent). No new disclosure vector — test proves the existing path captures tokens, does not extend it. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase. Test-only; vitest already a dev dependency. No new deps per CLAUDE.md. |
</threat_model>

<verification>
- `cd tests && npx vitest run unit/ollama-claude-compat.test.js` exits 0 (all COMP-01/02/03 + fallback guard contracts green)
- `cd tests && npx vitest run unit/ollama-claude-transport.test.js unit/ollama-claude-block-fidelity.test.js` green — Phase 1/2 contracts intact (no regression)
- No source files modified UNLESS a COMP-03 test revealed a usage-extraction gap (then the diff is 1-3 lines in an existing Claude-shape branch of usageTracking.js or sseToJsonHandler.js)
- Test file uses FORMATS constants, no hardcoded format strings
</verification>

<success_criteria>
- COMP-01: tool_choice (with non-empty tools) + metadata + rewritten cache_control survive translateRequest(claude→claude, ollama) — fields tolerated, not stripped
- COMP-02: ollama message_delta stop_reason values (tool_use/end_turn/max_tokens) pass through translateResponse(claude→claude) unchanged to the client
- COMP-03: ollama-shaped message_start (input_tokens) + message_delta (output_tokens) flow through extractUsage/mergeUsage into prompt_tokens/completion_tokens for gateway tracking
- Non-Claude fallback intact: resolveTransport returns null for openai source on ollama + ollama-local (openai path stays on /api/chat)
</success_criteria>

<output>
Create `.planning/phases/03-compatibility-fallback/03-01-SUMMARY.md` when done.
</output>