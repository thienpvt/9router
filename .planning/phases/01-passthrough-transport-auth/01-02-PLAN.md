---
phase: 01-passthrough-transport-auth
plan: 02
type: tdd
wave: 2
depends_on:
  - 01-01
files_modified:
  - open-sse/executors/ollama-local.js
  - tests/unit/ollama-claude-transport.test.js
autonomous: true
requirements:
  - PASS-02
  - PASS-03
  - COMP-04

must_haves:
  truths:
    - OllamaLocalExecutor.buildUrl returns <resolved-host>/v1/messages when credentials.runtimeTransport has a claude baseUrl
    - OllamaLocalExecutor.buildUrl honors user-configured host override (providerSpecificData.baseUrl) for the claude path, not just the registry literal
    - OllamaLocalExecutor.buildUrl falls back to <resolved-host>/api/chat when no runtimeTransport is set (openai/ollama-format path unchanged)
    - A runnable vitest file proves resolveTransport contracts + buildUrl generalization together
  artifacts:
    - open-sse/executors/ollama-local.js (generalized buildUrl)
    - tests/unit/ollama-claude-transport.test.js (new — the one runnable self-check per CLAUDE.md lazy-senior rule)
  key_links:
    - OllamaLocalExecutor.buildUrl reads credentials.runtimeTransport.baseUrl (set by chatCore.js:53 from resolveTransport)
    - resolveOllamaLocalHost (open-sse/config/providers.js:7) provides the host — must be honored for both the claude path and the fallback path
---

<objective>
Generalize `OllamaLocalExecutor.buildUrl` to honor `credentials.runtimeTransport` first (matching DefaultExecutor.buildUrl's runtimeTransport branch), substituting the user-resolved local host, then falling back to the existing `resolveOllamaLocalHost(credentials)/api/chat`. This makes the local claude transport's `/v1/messages` endpoint reachable when Plan 01-01's registry entry matches. Also leaves ONE runnable vitest self-check proving the three Phase 1 contracts: resolveTransport returns the /v1/messages transport for claude source, returns null for openai source (fallback preserved), and buildUrl yields <host>/v1/messages under a claude runtimeTransport.

Purpose: Deliver PASS-02's reachability half (the local claude transport declared in 01-01 is now actually hit by the executor) and PASS-03's guard (fallback path asserted in the test). The test is Phase 1's single runnable check per the CLAUDE.md lazy-senior rule; Phase 4 owns the comprehensive VAL-01/02/03 suite.
Output: One modified executor + one new test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-passthrough-transport-auth/01-CONTEXT.md
@.planning/phases/01-passthrough-transport-auth/01-PATTERNS.md
@.planning/phases/01-passthrough-transport-auth/01-01-SUMMARY.md
</context>

<feature>
  <name>Ollama local claude transport reachability + Phase 1 contract self-check</name>
  <files>open-sse/executors/ollama-local.js, tests/unit/ollama-claude-transport.test.js</files>
  <behavior>
    Contract A (from Plan 01-01, asserted here as a regression guard): resolveTransport("ollama","claude") returns a transport whose .format === "claude" and .baseUrl === "https://ollama.com/v1/messages"; resolveTransport("ollama-local","claude") returns .format === "claude" and .baseUrl === "http://localhost:11434/v1/messages".
    Contract B (PASS-03 fallback guard): resolveTransport("ollama","openai") === null AND resolveTransport("ollama-local","openai") === null — no openai transport advertised, so the default `transport` object (format:"ollama") is the fallback.
    Contract C (executor generalization, the TDD driver): OllamaLocalExecutor.buildUrl("", true, 0, {runtimeTransport:{baseUrl:"http://localhost:11434/v1/messages", format:"claude"}}) returns "http://localhost:11434/v1/messages".
    Contract C host-override: with credentials.providerSpecificData.baseUrl === "http://192.168.1.5:11434" and the same runtimeTransport, buildUrl returns "http://192.168.1.5:11434/v1/messages" (honors resolveOllamaLocalHost, not just the registry literal).
    Contract C fallback: buildUrl("", true, 0, null) returns "http://localhost:11434/api/chat" (no runtimeTransport → existing path unchanged).
    Cases (input → expected output):
      - {provider:"ollama-local", sourceFormat:"claude"} → resolveTransport returns baseUrl containing "/v1/messages"
      - {provider:"ollama-local", sourceFormat:"openai"} → resolveTransport returns null
      - {rt:{baseUrl:"http://localhost:11434/v1/messages"}, hostOverride:undefined} → buildUrl "http://localhost:11434/v1/messages"
      - {rt:{baseUrl:"http://localhost:11434/v1/messages"}, hostOverride:"http://192.168.1.5:11434"} → buildUrl "http://192.168.1.5:11434/v1/messages"
      - {rt:undefined, hostOverride:undefined} → buildUrl "http://localhost:11434/api/chat"
  </behavior>
  <implementation>
    In open-sse/executors/ollama-local.js, replace the buildUrl body so it first reads `const rt = credentials?.runtimeTransport;`. If rt?.baseUrl is truthy, compute the host via the existing `resolveOllamaLocalHost(credentials)` and the pathname via `new URL(rt.baseUrl).pathname`, then return `${host}${pathname}` (Option 2 from PATTERNS.md — honors user-configured host override; the registry baseUrl is a static literal but the host is user-overridable). Otherwise return the existing fallback `${resolveOllamaLocalHost(credentials)}/api/chat`. Keep the function signature `buildUrl(model, stream, urlIndex = 0, credentials = null)` unchanged. No new imports needed — resolveOllamaLocalHost is already imported at line 2. Do NOT override buildHeaders — OllamaLocalExecutor inherits DefaultExecutor.buildHeaders which already applies rt.headers + rt.auth (x-api-key raw + CLAUDE_API_HEADERS) when runtimeTransport is set.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing Phase 1 contract test</name>
  <files>tests/unit/ollama-claude-transport.test.js</files>
  <read_first>
    - tests/unit/capabilities.test.js (analog — vitest import pattern: `import { describe, expect, it } from "vitest";` with relative imports to ../../open-sse/)
    - tests/vitest.config.js (alias config — open-sse/ resolves to ../open-sse; relative ../../open-sse/ also works)
    - open-sse/services/provider.js (lines 142-147 — resolveTransport export)
    - open-sse/executors/ollama-local.js (current buildUrl — hardcodes /api/chat, so Contract C will fail RED)
    - open-sse/executors/default.js (lines 118-123 — parent buildUrl runtimeTransport branch being mirrored)
  </read_first>
  <behavior>
    - Test 1 (Contract A): resolveTransport("ollama","claude").baseUrl === "https://ollama.com/v1/messages" && .format === "claude"
    - Test 2 (Contract A local): resolveTransport("ollama-local","claude").baseUrl === "http://localhost:11434/v1/messages" && .format === "claude"
    - Test 3 (Contract B fallback): resolveTransport("ollama","openai") === null && resolveTransport("ollama-local","openai") === null
    - Test 4 (Contract C — expected RED here): new OllamaLocalExecutor().buildUrl("", true, 0, {runtimeTransport:{baseUrl:"http://localhost:11434/v1/messages", format:"claude"}}) === "http://localhost:11434/v1/messages"
    - Test 5 (Contract C host-override — expected RED): buildUrl with providerSpecificData.baseUrl "http://192.168.1.5:11434" === "http://192.168.1.5:11434/v1/messages"
    - Test 6 (Contract C fallback — passes now): buildUrl("", true, 0, null) === "http://localhost:11434/api/chat"
  </behavior>
  <action>
    Create tests/unit/ollama-claude-transport.test.js. Top of file: `import { describe, expect, it } from "vitest";` then import resolveTransport from "../../open-sse/services/provider.js", import OllamaLocalExecutor from "../../open-sse/executors/ollama-local.js". (Do NOT import the registry objects directly — resolveTransport reads the global PROVIDERS map via open-sse/providers/index.js which auto-imports both ollama.js and ollama-local.js; that import chain is triggered by importing provider.js.) Write one describe block "Phase 1: Claude passthrough transport" with six it blocks matching the behavior contracts above. Use expect(...).toBe(...) for exact-string assertions and expect(...).toBeNull() for the fallback. The buildUrl call site is `new OllamaLocalExecutor().buildUrl(model, stream, urlIndex, credentials)` — pass model="" stream=true urlIndex=0. Run the file via `npx vitest run unit/ollama-claude-transport.test.js` from tests/. Tests 1-3 and 6 should PASS (Plan 01-01 done); Tests 4-5 should FAIL RED (buildUrl still returns /api/chat — that is the expected RED state driving Task 2). Commit the failing test as test(01-02): add Phase 1 contract self-check.
  </action>
  <verify>
    <automated>cd tests && npx vitest run unit/ollama-claude-transport.test.js 2>&1 | tee /dev/stderr | grep -qE "(expected|FAIL).*ollama-claude-transport|Tests 4|Test 4" && echo "RED confirmed" || (cd tests && npx vitest run unit/ollama-claude-transport.test.js 2>&1 | grep -qiE "buildUrl|/v1/messages" && echo "test file runs")</automated>
  </verify>
  <acceptance_criteria>
    - File tests/unit/ollama-claude-transport.test.js exists
    - File imports {describe, expect, it} from "vitest", resolveTransport from "../../open-sse/services/provider.js", OllamaLocalExecutor from "../../open-sse/executors/ollama-local.js"
    - Contains assertions for all 6 contracts listed in behavior
    - `cd tests && npx vitest run unit/ollama-claude-transport.test.js` runs the file (collects without import errors)
    - Tests 4 and 5 (buildUrl claude path) FAIL at this stage (RED) — proves the executor change is needed
    - Tests 1, 2, 3, 6 PASS (registry + fallback already correct from 01-01)
  </acceptance_criteria>
  <done>Failing contract test committed; RED on buildUrl claude path confirmed; registry + fallback contracts already green from 01-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Generalize OllamaLocalExecutor.buildUrl to honor runtimeTransport</name>
  <files>open-sse/executors/ollama-local.js</files>
  <read_first>
    - open-sse/executors/ollama-local.js (current state — file being modified, lines 9-11)
    - open-sse/executors/default.js (lines 118-123 — parent buildUrl runtimeTransport branch to mirror)
    - open-sse/config/providers.js (lines 5-10 — resolveOllamaLocalHost contract: reads providerSpecificData.baseUrl, trims, strips trailing slash, falls back to OLLAMA_LOCAL_DEFAULT_HOST)
    - tests/unit/ollama-claude-transport.test.js (the RED test from Task 1 — this change must make Tests 4-5 pass)
  </read_first>
  <behavior>
    - buildUrl with runtimeTransport.baseUrl present returns <resolveOllamaLocalHost(credentials)> + <pathname of rt.baseUrl>
    - buildUrl without runtimeTransport returns <resolveOllamaLocalHost(credentials)>/api/chat (byte-identical to current behavior)
    - User host override via providerSpecificData.baseUrl is honored on the claude path
  </behavior>
  <action>
    Per D (CONTEXT: "local claude transport baseUrl resolved at request time via OllamaLocalExecutor.buildUrl override honoring credentials.runtimeTransport") and PATTERNS.md Option 2. In open-sse/executors/ollama-local.js, replace the single-line buildUrl return with a runtimeTransport-first branch. New body: declare `const rt = credentials?.runtimeTransport;`. When rt?.baseUrl is truthy, return the concatenation of `resolveOllamaLocalHost(credentials)` (the resolved host, honoring providerSpecificData.baseUrl override) and `new URL(rt.baseUrl).pathname` (the path portion of the registry-declared baseUrl, e.g. "/v1/messages"). This honors the user host override — the registry literal "http://localhost:11434/v1/messages" contributes only its pathname. When rt?.baseUrl is falsy, return the existing fallback `${resolveOllamaLocalHost(credentials)}/api/chat`. Keep the signature `buildUrl(model, stream, urlIndex = 0, credentials = null)` unchanged. resolveOllamaLocalHost is already imported at line 2 — no new import. Do NOT override buildHeaders (DefaultExecutor.buildHeaders already applies rt.headers + rt.auth). Re-run the Task 1 test — all 6 tests must now pass GREEN. Commit as feat(01-02): generalize OllamaLocalExecutor.buildUrl for claude transport.
  </action>
  <verify>
    <automated>cd tests && npx vitest run unit/ollama-claude-transport.test.js 2>&1 | grep -qiE "passed|0 failed" && echo "GREEN confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - buildUrl reads credentials?.runtimeTransport before falling back
    - When rt.baseUrl is set, returns `${resolveOllamaLocalHost(credentials)}${new URL(rt.baseUrl).pathname}` — honors host override
    - When rt is null/undefined or rt.baseUrl absent, returns `${resolveOllamaLocalHost(credentials)}/api/chat` (existing behavior preserved)
    - Function signature unchanged: buildUrl(model, stream, urlIndex = 0, credentials = null)
    - `cd tests && npx vitest run unit/ollama-claude-transport.test.js` passes all 6 tests (0 failed)
    - No buildHeaders override added (inherits parent)
  </acceptance_criteria>
  <done>All 6 contract tests pass; buildUrl honors claude runtimeTransport with host substitution and preserves the /api/chat fallback; PASS-02 local reachability and PASS-03 fallback guard both proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway→ollama-local | Executor constructs the upstream URL from registry transport + user-configured host (providerSpecificData.baseUrl) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | OllamaLocalExecutor.buildUrl host substitution | medium | mitigate | The claude-path URL is built from resolveOllamaLocalHost(credentials) — the same trust boundary as the existing /api/chat fallback. resolveOllamaLocalHost (open-sse/config/providers.js:7-10) reads providerSpecificData.baseUrl which is dashboard-configured by the user (not external client input). The pathname is extracted from the registry-declared baseUrl (hardcoded "/v1/messages") via new URL().pathname — a malformed rt.baseUrl throws synchronously rather than producing an SSRF-able URL. No client-controlled string reaches the host portion. |
| T-01-05 | Spoofing | runtimeTransport source | low | mitigate | credentials.runtimeTransport is set exclusively by chatCore.js:53 from resolveTransport(provider, sourceFormat) — it is never read from the client request body. A client cannot inject a custom runtimeTransport to redirect the upstream call. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages — pure JS edit to an existing executor + a new vitest file (vitest already a dev dep in tests/). |
</threat_model>

<verification>
- `cd tests && npx vitest run unit/ollama-claude-transport.test.js` — 6/6 tests pass
- Test proves Contract A (resolveTransport claude returns /v1/messages), Contract B (openai returns null → fallback preserved per PASS-03), Contract C (buildUrl yields <host>/v1/messages for claude runtimeTransport)
- Host-override case proves user-configured providerSpecificData.baseUrl is honored (not just the registry literal)
- Fallback case proves openai/ollama-format path unchanged
</verification>

<success_criteria>
- PASS-02 reachability: OllamaLocalExecutor.buildUrl routes the claude transport to <resolved-host>/v1/messages
- PASS-03 guard: fallback to /api/chat preserved and asserted by Test 6
- COMP-04 local: x-api-key raw auth applied by inherited DefaultExecutor.buildHeaders (rt.auth) — wired by convention, confirmed in Phase 4 (VAL-02)
- One runnable self-check left per CLAUDE.md lazy-senior rule (Phase 4 owns the comprehensive VAL suite)
</success_criteria>

<output>
Create `.planning/phases/01-passthrough-transport-auth/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `open-sse/executors/ollama-local.js` — `OllamaLocalExecutor.buildUrl` generalized: signature unchanged `buildUrl(model, stream, urlIndex = 0, credentials = null)`; body now reads `credentials?.runtimeTransport` first, returns `${resolveOllamaLocalHost(credentials)}${new URL(rt.baseUrl).pathname}` when set, else falls back to `${resolveOllamaLocalHost(credentials)}/api/chat`. No new exports. No buildHeaders override (inherits DefaultExecutor).
- `tests/unit/ollama-claude-transport.test.js` — new vitest file (the Phase 1 runnable self-check). Imports resolveTransport from open-sse/services/provider.js and OllamaLocalExecutor from open-sse/executors/ollama-local.js. Six assertions covering Contracts A (claude transport resolution for cloud + local), B (openai returns null → fallback preserved), C (buildUrl claude path + host override + /api/chat fallback).