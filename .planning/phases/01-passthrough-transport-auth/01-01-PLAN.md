---
phase: 01-passthrough-transport-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - open-sse/providers/registry/ollama.js
  - open-sse/providers/registry/ollama-local.js
autonomous: true
requirements:
  - PASS-01
  - PASS-02
  - PASS-03
  - COMP-04

must_haves:
  truths:
    - resolveTransport("ollama","claude") returns a transport object with format:"claude" and baseUrl:"https://ollama.com/v1/messages"
    - resolveTransport("ollama-local","claude") returns a transport object with format:"claude" and baseUrl containing "/v1/messages"
    - resolveTransport("ollama","openai") returns null (no openai transport advertised → fallback to default transport object)
    - resolveTransport("ollama-local","openai") returns null (same fallback)
    - The existing transport object on both registries is byte-identical to pre-change (format:"ollama", baseUrl unchanged)
    - Both claude transport entries carry headers spread from CLAUDE_API_HEADERS and auth:{combined:true,header:"x-api-key",scheme:"raw"}
  artifacts:
    - open-sse/providers/registry/ollama.js (with transports[] array)
    - open-sse/providers/registry/ollama-local.js (with transports[] array)
  key_links:
    - resolveTransport (open-sse/services/provider.js:142) reads config.transports → must match shape
    - chatCore.js:51-53 sets credentials.runtimeTransport from resolveTransport → transports[].format must equal client sourceFormat
    - default.js buildHeaders:163-169 reads rt.headers + rt.auth → CLAUDE_API_HEADERS + x-api-key raw applied automatically
---

<objective>
Add a `transports[]` array with a single `claude`-format entry to both Ollama provider registries (cloud + local), mirroring the GLM multi-endpoint pattern. This makes `resolveTransport("ollama","claude")` match so `targetFormat` becomes `"claude"` and translation is skipped (same-format short-circuit at translator/index.js:78). The existing `transport` object stays byte-identical as the openai/ollama-format fallback (PASS-03).

Purpose: Deliver PASS-01 (cloud claude transport), PASS-02 (local claude transport), PASS-03 (fallback preserved — existing transport untouched), and COMP-04 (auth scheme x-api-key raw wired via transport.auth, applied by default.js buildHeaders).
Output: Two modified registry files with transports[] arrays; no new files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-passthrough-transport-auth/01-CONTEXT.md
@.planning/phases/01-passthrough-transport-auth/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add claude transport to Ollama Cloud registry (PASS-01, COMP-04)</name>
  <files>open-sse/providers/registry/ollama.js</files>
  <read_first>
    - open-sse/providers/registry/ollama.js (current state — file being modified)
    - open-sse/providers/registry/glm.js (analog — multi-endpoint template with transports[])
    - open-sse/providers/shared.js (lines 24-30 — CLAUDE_API_HEADERS constant definition)
    - open-sse/services/provider.js (lines 142-147 — resolveTransport reads config.transports, matches t.format === sourceFormat)
  </read_first>
  <behavior>
    - resolveTransport("ollama","claude") returns {format:"claude", baseUrl:"https://ollama.com/v1/messages", headers:{...CLAUDE_API_HEADERS}, auth:{combined:true,header:"x-api-key",scheme:"raw"}}
    - resolveTransport("ollama","openai") returns null (no openai entry in transports[])
    - The existing transport object (baseUrl:"https://ollama.com/api/chat", format:"ollama", validateUrl) is unchanged
  </behavior>
  <action>
    Per D (CONTEXT Transport & Registry Shape) and COMP-04: Add an import of CLAUDE_API_HEADERS from "../shared.js" at line 1 of the file (mirror glm.js line 1: `import { CLAUDE_API_HEADERS } from "../shared.js";`). Then add a `transports` array property to the exported object, placed immediately after the existing `transport` object (after line 22, before `models`). The array contains one entry with these exact fields: format set to the string "claude", baseUrl set to "https://ollama.com/v1/messages", headers spread from CLAUDE_API_HEADERS, auth object with combined true, header "x-api-key", scheme "raw". Do NOT add urlSuffix (ollama /v1/messages takes no query beta flag per CONTEXT). Do NOT add a usage field (ollama cloud usage is handled via the existing transport path). Do NOT modify the existing transport object — it stays byte-identical (baseUrl "https://ollama.com/api/chat", validateUrl, format "ollama") so the openai/ollama-format fallback is preserved per PASS-03. Do NOT hand-edit open-sse/providers/registry/index.js (auto-generated; adding transports[] to an existing provider file does not change the static import list so no regen is needed).
  </action>
  <verify>
    <automated>node -e "import('./open-sse/providers/registry/ollama.js').then(m => { const r = m.default; const t = r.transports.find(x => x.format==='claude'); console.assert(t &amp;&amp; t.baseUrl==='https://ollama.com/v1/messages' &amp;&amp; t.auth.header==='x-api-key' &amp;&amp; t.auth.scheme==='raw', 'claude transport missing'); console.assert(r.transport.format==='ollama' &amp;&amp; r.transport.baseUrl==='https://ollama.com/api/chat', 'fallback broken'); console.assert(!r.transports.find(x => x.format==='openai'), 'unexpected openai transport'); console.log('ollama.js OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - File imports CLAUDE_API_HEADERS from "../shared.js" (line 1)
    - transports[] array exists with exactly one entry where format==="claude"
    - transports[0].baseUrl === "https://ollama.com/v1/messages" (exact string)
    - transports[0].headers contains "Anthropic-Version" and "Anthropic-Beta" keys (spread from CLAUDE_API_HEADERS)
    - transports[0].auth === {combined:true, header:"x-api-key", scheme:"raw"}
    - transports[0] has NO urlSuffix property
    - transport object unchanged: transport.baseUrl==="https://ollama.com/api/chat", transport.format==="ollama", transport.validateUrl==="https://ollama.com/api/tags"
    - No openai-format entry in transports[]
  </acceptance_criteria>
  <done>Ollama Cloud registry advertises a claude transport at https://ollama.com/v1/messages with x-api-key raw auth and CLAUDE_API_HEADERS; existing ollama-format transport untouched as fallback (PASS-01, PASS-03, COMP-04 cloud).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add claude transport to Ollama Local registry (PASS-02, COMP-04 local)</name>
  <files>open-sse/providers/registry/ollama-local.js</files>
  <read_first>
    - open-sse/providers/registry/ollama-local.js (current state — file being modified)
    - open-sse/providers/registry/glm.js (analog — multi-endpoint template)
    - open-sse/providers/shared.js (lines 24-30 — CLAUDE_API_HEADERS)
    - open-sse/services/provider.js (lines 142-147 — resolveTransport contract)
    - open-sse/config/providers.js (lines 5-10 — resolveOllamaLocalHost; host is resolved at request time by the executor, not the registry)
  </read_first>
  <behavior>
    - resolveTransport("ollama-local","claude") returns {format:"claude", baseUrl:"http://localhost:11434/v1/messages", headers:{...CLAUDE_API_HEADERS}, auth:{combined:true,header:"x-api-key",scheme:"raw"}}
    - resolveTransport("ollama-local","openai") returns null
    - The existing transport object (baseUrl:"http://localhost:11434/api/chat", format:"ollama") is unchanged
  </behavior>
  <action>
    Per D (CONTEXT Transport & Registry Shape) and PASS-02: Add an import of CLAUDE_API_HEADERS from "../shared.js" at line 1 (mirror glm.js line 1). Add a `transports` array property to the exported object, placed immediately after the existing `transport` object (after line 17, before `serviceKinds`). The array contains one entry with: format "claude", baseUrl "http://localhost:11434/v1/messages" (static default literal — the actual host is substituted at request time by OllamaLocalExecutor.buildUrl via resolveOllamaLocalHost, which Plan 01-02 generalizes; the registry declares the default literal matching the existing transport.baseUrl host), headers spread from CLAUDE_API_HEADERS, auth object {combined:true, header:"x-api-key", scheme:"raw"} (per CONTEXT discretion: keep shape consistent with cloud; local ollama does not validate keys but if a key is configured it is sent — default.js applyAuth writes the header only when a key is present). Do NOT add urlSuffix. Do NOT modify the existing transport object (baseUrl "http://localhost:11434/api/chat", format "ollama") — it stays byte-identical as the fallback per PASS-03.
  </action>
  <verify>
    <automated>node -e "import('./open-sse/providers/registry/ollama-local.js').then(m => { const r = m.default; const t = r.transports.find(x => x.format==='claude'); console.assert(t &amp;&amp; t.baseUrl==='http://localhost:11434/v1/messages' &amp;&amp; t.auth.header==='x-api-key', 'claude transport missing'); console.assert(r.transport.format==='ollama' &amp;&amp; r.transport.baseUrl==='http://localhost:11434/api/chat', 'fallback broken'); console.log('ollama-local.js OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - File imports CLAUDE_API_HEADERS from "../shared.js" (line 1)
    - transports[] array exists with exactly one entry where format==="claude"
    - transports[0].baseUrl === "http://localhost:11434/v1/messages" (exact string)
    - transports[0].headers contains "Anthropic-Version" and "Anthropic-Beta" keys
    - transports[0].auth === {combined:true, header:"x-api-key", scheme:"raw"}
    - transports[0] has NO urlSuffix property
    - transport object unchanged: transport.baseUrl==="http://localhost:11434/api/chat", transport.format==="ollama"
  </acceptance_criteria>
  <done>Ollama Local registry advertises a claude transport at http://localhost:11434/v1/messages with x-api-key raw auth shape; existing ollama-format transport untouched as fallback (PASS-02, PASS-03, COMP-04 local).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→gateway | Claude-format request crosses into gateway (existing boundary, unchanged) |
| gateway→ollama-cloud | API key sent to https://ollama.com/v1/messages (new endpoint, same host) |
| gateway→ollama-local | Optional API key sent to http://localhost:11434/v1/messages (loopback) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | ollama.js claude transport auth | medium | mitigate | Auth scheme is x-api-key raw (Anthropic-compat convention, matches GLM). baseUrl hardcoded to https://ollama.com/v1/messages — no user-controlled baseUrl injection on cloud transport; API key only sent to the declared ollama.com host. |
| T-01-02 | Information Disclosure | ollama-local.js claude transport | low | accept | Local transport sends optional x-api-key over http (loopback). Loopback traffic never leaves the host; user's own machine. Acceptable per CONTEXT (local ollama does not validate keys). |
| T-01-03 | Tampering | transports[] shape | low | mitigate | transports[].format values are hardcoded lowercase strings ("claude"); resolveTransport does exact match against client sourceFormat — no user input reaches transport selection. Existing transport object untouched so openai/ollama fallback path is byte-identical (PASS-03 guard). |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed in this plan — pure registry config edit, zero deps added. |
</threat_model>

<verification>
- Both registry files import CLAUDE_API_HEADERS and expose a transports[] with one claude entry
- resolveTransport("ollama","claude") and resolveTransport("ollama-local","claude") return the /v1/messages transport
- resolveTransport("ollama","openai") and resolveTransport("ollama-local","openai") return null (fallback to default transport object preserved)
- Existing transport objects byte-identical (PASS-03)
- Auth scheme x-api-key raw on both (COMP-04)
</verification>

<success_criteria>
- PASS-01: Ollama Cloud registry advertises claude transport at https://ollama.com/v1/messages
- PASS-02: Ollama Local registry advertises claude transport at http://localhost:11434/v1/messages
- PASS-03: Existing ollama-format transport unchanged on both registries
- COMP-04: Auth scheme x-api-key raw wired via transport.auth on both claude entries
</success_criteria>

<output>
Create `.planning/phases/01-passthrough-transport-auth/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `open-sse/providers/registry/ollama.js` — new `transports[]` array with one `{format:"claude", baseUrl:"https://ollama.com/v1/messages", headers:{...CLAUDE_API_HEADERS}, auth:{combined:true,header:"x-api-key",scheme:"raw"}}` entry; new top-of-file import `import { CLAUDE_API_HEADERS } from "../shared.js";`. Existing `transport` object unchanged.
- `open-sse/providers/registry/ollama-local.js` — new `transports[]` array with one `{format:"claude", baseUrl:"http://localhost:11434/v1/messages", headers:{...CLAUDE_API_HEADERS}, auth:{combined:true,header:"x-api-key",scheme:"raw"}}` entry; new top-of-file import `import { CLAUDE_API_HEADERS } from "../shared.js";`. Existing `transport` object unchanged.
- No new exported symbols. The `transports` property is read by `resolveTransport` (open-sse/services/provider.js:142) via `config.transports`.